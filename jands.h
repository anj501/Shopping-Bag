#include <iostream>
#include <string>
#include <vector>
using namespace std;

class jands {
    public: 
        void opener(int& total, vector<string>& item, vector<int>& icost);
        void addtobag(string reply, int& total, vector<string>& item, vector<int>& icost);
};